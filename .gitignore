# Ignore Python cache files
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so

# Ignore virtual environments
.venv/
env/
venv/
ENV/

# Ignore Jupyter notebook checkpoints
.ipynb_checkpoints/

# Ignore system files
.DS_Store
Thumbs.db

# Ignore dataset files (too large for GitHub)
data/*.csv
*.zip

# Ignore log files
*.log

# Ignore model files if large
*.h5
*.pkl
*.joblib


#Explanation:

#__pycache__/, *.pyc → compiled Python files (not needed in repo)

#.venv/, env/ → your local virtual environment (others will create their own)

#.ipynb_checkpoints/ → Jupyter autosave junk

#data/*.csv → Titanic dataset (users can download from Kaggle instead of uploading big CSV files to GitHub)

#*.pkl, *.joblib → trained model files (optional to share, usually excluded if very large
